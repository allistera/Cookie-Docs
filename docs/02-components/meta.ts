import { defineMeta } from 'blume';

export default defineMeta({
  title: 'Components',
  icon: 'layout-grid',
  pages: ['cookie-web', 'cookie-worker', 'cookie-ios', 'cookie-docs', 'cookie-agents'],
});
